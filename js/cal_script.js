function c(ip) {
		let b = ip.split('');
		b.pop();
		return b.join('');
	}
// function clearLast() {
//     var inputField = document.getElementsByName('inptext')[0];
//     var currentValue = inputField.value;
//     inputField.value = currentValue.slice(0, -1); // Remove the last character
// }
